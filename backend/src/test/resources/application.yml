spring:
  datasource:
    url: jdbc:h2:mem:roamfx;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
app:
  jwtSecret: test-secret-test-secret-test-secret-test-secret
  jwtExpiryMinutes: 60
  allowedOrigins: http://localhost:3000
  rateLockMinutes: 30
  suspiciousDeviationPercent: 8
